import styled from 'styled-components'
import { Box } from '../../sharedStyles/containers'
import { BlueButton } from '../../sharedStyles/buttons';

export const RedirectBox = styled(Box)`
  text-align: center;
`

export const RedirectButton = styled(BlueButton)`
  margin-bottom: 1rem;
`
